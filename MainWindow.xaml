<Window x:Class="WpfShop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Магазин запчастей ИЖ - Вход в систему" 
        Height="650" Width="400"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="OrangeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF9800"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F57C00"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#F57C00"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#FFB74D"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="OrangeTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#FFB74D"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,30"/>
        </Style>
        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Grid Background="#FFF8F2">
        <Border Background="White" Margin="30" Padding="30" BorderBrush="#FFB74D" BorderThickness="1" CornerRadius="12">
            <StackPanel>
                <TextBlock Style="{StaticResource TitleTextStyle}" Text="Администратор"/>
                <Viewbox Width="80" Height="80" Margin="0,0,0,20" HorizontalAlignment="Center">
                    <Canvas Width="24" Height="24">
                        <Path Fill="#FF9800" Data="M6,17C6,15 10,13.9 12,13.9C14,13.9 18,15 18,17V18H6M15,9A3,3 0 0,1 12,12A3,3 0 0,1 9,9A3,3 0 0,1 12,6A3,3 0 0,1 15,9M3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5A2,2 0 0,0 19,3H5C3.89,3 3,3.9 3,5Z"/>
                    </Canvas>
                </Viewbox>
                <StackPanel Margin="0,20,0,0">
                    <TextBlock Style="{StaticResource LabelTextStyle}" Text="Номер телефона"/>
                    <TextBox x:Name="PhoneTextBox" Style="{StaticResource OrangeTextBoxStyle}" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" MaxLength="16"/>
                    <TextBlock Style="{StaticResource LabelTextStyle}" Text="Пароль" Margin="0,20,0,0"/>
                    <PasswordBox x:Name="PasswordBox" BorderBrush="#FFB74D" BorderThickness="2" Padding="12,8" FontSize="14"/>
                    <Button x:Name="LoginButton" Style="{StaticResource OrangeButtonStyle}" Content="Войти в систему" Margin="0,30,0,10" Height="45" FontSize="16" Command="{Binding LoginCommand}"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="13" FontWeight="Medium" HorizontalAlignment="Center" TextWrapping="Wrap" Margin="0,15,0,0" Foreground="{Binding StatusColor}"/>
                </StackPanel>
                <Border Height="1" Background="#FFB74D" Margin="0,25,0,25" Opacity="0.5"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>